# OpenAI API key (required)
OPENAI_API_KEY=sk-your-api-key-here

# Storage path for projects (optional, defaults to ~/.konte)
STORAGE_PATH=~/.konte

# Embedding model (optional)
EMBEDDING_MODEL=text-embedding-3-small

# Context generation model (optional)
CONTEXT_MODEL=gpt-4.1

# Custom Backend (vLLM with OpenAI-compatible API)
# When both BACKENDAI_ENDPOINT and BACKENDAI_MODEL_NAME are set,
# context generation and answer generation use the custom backend instead of OpenAI.
# Default values are set in settings.py, override here if needed:
#
# Option 1: Gemma 3 27B (recommended for evaluation)
# BACKENDAI_ENDPOINT=https://xaas_model.asia03.app.backend.ai/v1
# BACKENDAI_MODEL_NAME=gemma-3-27b-it
#
# Option 2: Qwen3-VL-8B
# BACKENDAI_ENDPOINT=https://qwen25vl.asia03.app.backend.ai/v1
# BACKENDAI_MODEL_NAME=Qwen3-VL-8B-Instruct
#
# BACKENDAI_API_KEY=optional-api-key
